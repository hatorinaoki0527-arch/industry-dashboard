const {test}=require('node:test'),a=require('node:assert/strict'),C=require('../candles.js');
test('calendar years clamp leap days, exclude future and retain daily bars',()=>{a.equal(C.startDate('2024-02-29',1),'2023-02-28');a.equal(C.startDate('2026-09-21',5),'2021-09-21');const rows=[{date:'2021-09-20'},{date:'2021-09-21'},{date:'2026-09-21'},{date:'2026-09-22'}];a.deepEqual(C.windowData(rows,'2026-09-21',5).rows.map(r=>r.date),['2021-09-21','2026-09-21']);});
test('invalid or missing OHLC never becomes synthetic candle',()=>{for(const r of [{close:10},{open:10,high:9,low:8,close:10},{open:0,high:10,low:0,close:9}])a.equal(C.isBar(r),false);const html=C.render([{date:'2026-09-21',close:10}],{cutoff:'2026-09-21'});a.match(html,/暂无完整/);a.doesNotMatch(html,/data-candle=/);});
test('year ranges render five controls and MA uses pre-window data',()=>{const rows=Array.from({length:400},(_,i)=>({date:new Date(Date.UTC(2025,0,1+i)).toISOString().slice(0,10),open:100+i,high:102+i,low:99+i,close:101+i}));const html=C.render(rows,{cutoff:rows.at(-1).date,years:1});a.equal((html.match(/data-k-years=/g)||[]).length,5);a.match(html,/data-k-years="1" aria-pressed="true"/);a.match(html,/MA90/);a.equal((html.match(/data-candle=/g)||[]).length,366);a.match(html,/data-ma-line="20"[^>]*d="M/);a.match(html,/class="up" data-candle/);});
